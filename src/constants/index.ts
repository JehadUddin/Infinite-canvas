import { ShapeType } from '../types';

export const allShapeTypes: ShapeType[] = ['rectangle', 'ellipse', 'cylinder', 'terminator', 'decision', 'star'];

export const MIN_ZOOM = 0.1;
export const MAX_ZOOM = 5;
export const ZOOM_STEP = 0.1;

export const DEFAULT_ITEM_WIDTH = 150;
export const DEFAULT_ITEM_HEIGHT = 150;

export const DEFAULT_STICKY_NOTE_WIDTH = 150;
export const DEFAULT_STICKY_NOTE_HEIGHT = 150;

export const DEFAULT_TEXT_WIDTH = 150;
export const DEFAULT_TEXT_HEIGHT = 40;

export const DEFAULT_FRAME_WIDTH = 500;
export const DEFAULT_FRAME_HEIGHT = 300;
